[project]
name = "zd-34529-sandbox-harness"
version = "0.1.0"
description = "Python port of the Sandbox Orchestration Harness (ticket 34529)"
requires-python = ">=3.9"
dependencies = ["temporalio>=1.15.0,<2"]

[tool.hatch.build.targets.wheel]
packages = ["core", "workers", "starter"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
