

temporal_namespace=antonio.a2dd6
#temporal_namespace=default

### Temporal worker run in a docker container in the same network as the Temporal server
temporal_worker_target_endpoint=antonio.a2dd6.tmprl.cloud:7233
#temporal_worker_target_endpoint=temporal:7233
### Temporal starter run as a maven/java application , it might need a different url to connect to the Temporal server
temporal_starter_target_endpoint=antonio.a2dd6.tmprl.cloud:7233
#temporal_starter_target_endpoint=localhost:7233




### IF
temporal_local_server=false

### ELSE

# Location of your key certificate, which should look like:
# -----BEGIN PRIVATE KEY-----
# ...
# -----END PRIVATE KEY-----
temporal_key_location=/certs/client.key
# Location of your client certificate, which should look like:
# -----BEGIN CERTIFICATE-----
# ...
# -----END CERTIFICATE-----
temporal_cert_location=/certs/client.pem

### ENDIF